<%= render  :partial => "shared/devise/passwords/edit/form",
            :locals => {  :resource => resource,
                          :resource_name => resource_name,
                          :path => password_path(resource_name),
                          } %>
