<table class="table table-bordered">
  <% @jobs.each do |job| %>
    <tr>
      <td><%= job.title %></td>
      <td><%= link_to "View", employers_job_dashboard_path(job) %></td>
    </tr>
  <% end %>
</table>
